declare module "@mui/material/styles" {
  interface Theme {
    colors: {
      first: string;
      second: string;
      third: string;
      textFirst: string;
      textSecond: string;

      button: string;

      featured: {
        plan: string;
        upgrade: string;
        arrow: string;
      };
    };
  }

  interface ThemeOptions {
    colors: {
      first: string;
      second: string;
      third: string;
      textFirst: string;
      textSecond: string;

      button: string;

      featured: {
        plan: string;
        upgrade: string;
        arrow: string;
      };
    };
  }
}
